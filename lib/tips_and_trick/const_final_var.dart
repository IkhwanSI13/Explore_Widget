import 'package:flutter/material.dart';

/// https://github.com/bizz84/flutter-tips-and-tricks/blob/main/tips/0071-const-vs-final-vs-var/index.md
/// const is for hardcoded, compile-time constants
/// final is for read-only variables that are set just once
/// var is for variables that are set more than once
class ConstFinalVar extends StatelessWidget {
  ConstFinalVar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
